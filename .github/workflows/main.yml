on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: [self-hosted, windows, x64] 
    steps:
      - name: Checkout
        uses: actions/checkout@v4
